Before calling randomuser_get, make sure that you have the required values provided by the user. Required Values: gender (male, female), nat (e.g., us, gb). Optional Values: results (integer, default 1). If the user has not provided the require values, ask them to provide them. 